# Add-ons

Add-ons define optional capabilities that layer on top of profiles.

Current families:

- databases
- orchestration

Use `python scripts/new_project.py --list-addons` or the shell wrappers to see the current list.
