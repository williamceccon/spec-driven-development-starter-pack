# Profiles

Profiles define the project shape.

- ready profiles are available in the guided generator today
- planned profiles are catalog placeholders for future releases

Use `python scripts/new_project.py --list-profiles` or the shell wrappers to see the current list.
