# Core Workflow Pack

This directory contains starter-pack assets that are stack-agnostic:

- shared generated documentation templates
- portable onboarding content
- machine-readable contract defaults

Profiles add stack shape. Add-ons add optional capabilities. The core keeps the
workflow contract stable across all generated repositories.
